# Full runtime — core + optional features. Each of these is try-import
# guarded at its call site and degrades to a logged "feature unavailable"
# instead of crashing the process when missing (see cc-tasks/92 §1).
-r requirements-core.txt

aiosmtplib>=3.0.0
python-socks[asyncio]>=2.5.0
ddgs>=9.0.0
gradio_client>=1.3.0
pillow>=10.3.0
pillow-heif>=0.18.0
pypdf>=4.2.0
python-chess>=1.999
python-docx>=1.1.2
mcp>=1.9.0
psutil>=5.9.0
mss>=9.0.1
rapidocr-onnxruntime>=1.3.0

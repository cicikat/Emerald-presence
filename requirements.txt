# Umbrella file kept for `pip install -r requirements.txt` (dev/manual-clone
# convenience). Release packaging uses requirements.lock, compiled from
# requirements-full.txt — see cc-tasks/92 §1/§2.
-r requirements-test.txt

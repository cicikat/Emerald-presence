[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    audit: 迁移/重构验收快照，行为不测代码结构（Brief 50 · 工单F.4，先注册，打标签在触碰 C/D/E 相关文件时顺手做）
    contract: 跨模块/跨仓契约测试（Brief 50 · 工单F.4）
    smoke: 轻量冒烟测试（Brief 50 · 工单F.4）

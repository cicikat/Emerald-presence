# 00 · 代办优先级与依赖梳理总表

> 本文是这批代办的**总览与调度文档**，不含施工细节。
> 角色分工：本文 + 后续每项的 brief 由「文档侧」产出；CC 按 brief 施工。
> 目的有三：① 把你随手记的 18 条翻译成**底层需求**（你说过有些点只是示例）；
> ② 标清**谁挡谁**，避免乱序施工返工；③ 把「要不要做」的**决策项**单独拎出来，等你拍板。
>
> **本文不做任何代码改动判定**，只做分类、排序、依赖、和「待你确认」。逐项 brief 在你确认波次后再写。

---

## 0. 仓库与定位

| 仓库 | 路径 | 这批里的归属 |
|---|---|---|
| 后端 / 核心 | `D:\ai\Emerald-presence\` | 注入管线、记忆、梦境、溯源、召回、工具——**14 项** |
| 桌面客户端 | `D:\ai\Emerald-client\` | html 渲染、标题轮询、玩具窗口——**3 项** |
| 跨仓 | 两边 | reality 破限/世界书加 id + 前端统一——**1 项** |

注入管线枢纽：`core/prompt_builder.py`（已通读，下面的层号都以它为准）。

---

## 1. 全量清单 · 原话 → 底层需求 → 落点

> 「类型」三类：**FIX**=方向已明确，等 brief 直接施工；**诊断**=先查清现状再决定改不改；**决策**=要不要做本身待你拍板（单独出决策备忘）。
> 编号 P=注入/记忆管线，D=梦境，G=记忆治理，F=前端，X=决策。

### 注入管线簇

| 编号 | 你的原话 | 底层需求（解读） | 类型 | 落点 |
|---|---|---|---|---|
| **P1** | promptbuilder 小改顺序；条件注入缩短精简到「条」；用户特意提到的（如生日）单独注入尾部并加注释 | 这其实是三件事：① 注入层**重排**；② 把一堆 `tagged` 条件层（3.5 经期 / 3.6 watch / 3.8 活动 / 3.9 屏幕 / 6d 日记…）从多句段落**压成一行一条**；③ 给「**用户显式标记的高价值事实**」开一个**独立、稳定、带注释的尾部注入位**——生日只是例子，本质是这类事实不该混在泛化画像里被一起裁剪。P1 是注入侧重构的**骨架**，P3/P4/P5/D3 的改动都要落进它。 | FIX（骨架） | `prompt_builder.py` 层 1.5–6g、新增尾部层 |
| **P2** | tool 信息选择性注入短期记忆（日记要注入并查重、别二次触发同一文件；时间这种不注入） | 工具结果回写短期记忆要**幂等 + 分类**：把工具产物分「**可沉淀**」（日记内容→写入短期记忆，并按文件指纹去重，避免同一篇被反复读+反复注入）与「**易失**」（时间/天气→只用本轮，不沉淀）。 | FIX | `prompt_builder.py` 层 10、`core/tools/diary_*.py`、`core/memory/short_term.py` |
| **P3** | 用户画像改单条条件注入；筛 tag（如「喜欢听的歌」单独 likesing+时间戳，召回只召最近，除非明确提到几个月前的） | 层 5「关于这个用户」目前是**全字段 100% 平铺注入**。需求是**类型化 + 条件化**：画像条目带 **type/tag + 时间戳**，注入改成**按相关性单条召回**；偏好类（likesing 这类）默认只召**最近**，除非用户明确点名旧偏好。likesing 只是示例 tag，本质是给画像引入「**条目模型 + recency 召回**」。 | FIX | `prompt_builder.py` 层 5、`core/memory/user_profile.py`、`user_facts.py` |
| **P4** | reality 的破限和世界书也加 id，前端统一 | 破限条目（`characters/reality/jailbreaks/*.json`）和世界书条目目前**无稳定 id**，前端难以稳定引用/编辑/排序/溯源。需求：**给条目加稳定 id**（后端 schema），**前端统一管理 UI**。这是 P 簇里唯一**跨仓**项。 | FIX（跨仓） | 后端 jailbreak/lore schema + `_load_jailbreak`；client 前端编辑器 |
| **P5** | 各 prompt 里「用户」称呼（如 midterm）改「她」；注入前加个清洗 | midterm 只是**症状点**，根因是注入文本里散落的「用户 / user」是**第三人称系统口吻**，会把角色语感带偏。需求是**一个统一的「注入前文本规范化层」**（normalize hook），集中处理称呼替换/口吻清洗，而不是逐处手改。挂在 P1 骨架上。 | FIX | `prompt_builder.py`（新增 normalize 钩子）、`core/memory/mid_term.py` |

### 梦境簇

| 编号 | 你的原话 | 底层需求（解读） | 类型 | 落点 |
|---|---|---|---|---|
| **D1** | 查梦境模式里 mes_example 一直在 fallback | **先诊断**：找出梦境 prompt 为什么总走 mes_example 兜底（卡字段缺失？dream 专用构建器没读到？解析失败？）。结论可能是一行修，也可能牵出卡加载问题。 | 诊断 | `core/dream/dream_prompt.py`、`dream_pipeline.py` |
| **D2** | dream_impression 的写入要大修 | **症状已明确（你确认）：印象太模糊 → 给模型留幻觉空间**。方向 = **调细粒度，写得更具体**：现实可以记得梦的**剧情**和**部分清晰的话语**（更像真实梦）。原先怕"梦境混淆现实"已排除——梦境回忆注入层有明确标签兜底，所以放心写具体。写入链重构围绕"模糊概括 → 具体片段+原话摘录"展开。 | FIX（大改） | `core/dream/distill_impression.py`、`impression_store.py` |
| **D3** | 梦境模式现实初始注入（入梦前背景）在一定轮数后自然丢弃，避免带歪语感；概括性现实记忆不变（「你记得入梦前在做 xxx」） | 梦境里**逐字的现实初始上下文**应随轮数衰减后**停止注入**（防止把现实语感带进梦），但**概括化的一句**保留。需求 = 给梦境现实注入加**轮数 TTL + 降级为概括**两段式。 | FIX | `core/dream/dream_context.py`、`dream_prompt.py` |

### 记忆治理簇

| 编号 | 你的原话 | 底层需求（解读） | 类型 | 落点 |
|---|---|---|---|---|
| **G1** | 查日记自动总结跑哪去了，现在还生效吗 | **诊断**：`core/scheduler/triggers/diary.py` 存在，需确认它是否注册、是否在跑、产物落哪、有没有静默失败。结论决定 P2（日记注入）是否建立在一个已死的链路上。 | 诊断 | `core/scheduler/triggers/diary.py`、`core/dream/dream_summary.py` |
| **G2** | 显式遗忘/覆盖做了吗？感觉是重灾区，检查一下 | **诊断 + 大概率补做**：当前各记忆存储（profile/identity/episodic/event_log）有没有「用户说『忘了它』就生效」的**显式遗忘/覆盖**通道。初查未见统一机制。先盘点现状，再决定补哪几层。 | 诊断→FIX | `core/memory/*`（user_profile / user_identity / episodic_memory / event_log） |
| **G3** | 溯源：已做回复+探针溯源，没做「改动印象溯源」（identity 和 midterm 的概括溯源） | 现有 `_provenance` 只标了**注入来源**（哪层、什么 query 召回的）。缺的是**写入侧溯源**：identity / midterm 这类**概括被更新/漂移时，记下是哪轮对话、哪条原始信号导致的改动**。这是「记忆为什么变成这样」的可追溯性。 | 决策→FIX | `core/observe/prompt_capture.py`、`user_identity.py`、`mid_term.py` |
| **X4** | 要不要做 yexuan-self，专门放叶瑄被用户改变/漂移的部分？（你也怀疑来不及） | 决策项。与 G3 强相关（都是「漂移」）。先在决策备忘里评估：是独立存储，还是 G3 溯源的一个视图；以及「来不及」是否成立。 | 决策 | 新模块 / 复用 G3 |

### 召回质量

| 编号 | 你的原话 | 底层需求（解读） | 类型 | 落点 |
|---|---|---|---|---|
| **X1** | 要不要建向量库？tag 有点粗粒度了 | **决策项，且是本批的依赖根**。现状（已核对）：`event_log.search` 用**关键词重叠**算相关性（`hit / len(keywords)`）+ 强度×时间衰减；episodic 提过 embedding 但 `long_term_rag` 只剩 .pyc（疑似已下线）。是否建向量库直接决定 P3（画像召回）、X2（query relevance 评分）、G3（溯源是否向量化）怎么实现。**必须最先拍板**。 | 决策 | `core/memory/episodic_memory.py`、`event_log.py` |
| **X2** | 召回质量：召回评分缺 query relevance（opus 早提过） | 现有评分以**强度×衰减**为主，query 相关性只是粗糙的关键词命中率。需求是把 **query relevance 做成召回评分的一等公民**。**实现方式取决于 X1**（向量库→语义相似度；否则→更好的词级/重排）。 | FIX（依赖 X1） | `event_log.py` search、`episodic_memory.py` |

### 前端 / 客户端

| 编号 | 你的原话 | 底层需求（解读） | 类型 | 落点 |
|---|---|---|---|---|
| **F1** | html 没生效 | **诊断**：层 11 会注入词级标记 `<hl>/<big>/<sm>`，需确认前端 ChatPanel 是否解析渲染、还是当纯文本显示。先定位「html」具体指这套词级标记还是别的富文本。 | 诊断 | `Emerald-client` `ChatPanel.tsx` |
| **F2** | 前台标题轮询也没怎么见了 | **诊断**：窗口标题/通知轮询疑似失效或被移除。先查现状再决定修不修。 | 诊断 | `Emerald-client` chat window |
| **F3** | 玩具文件（veryformalproject）叶瑄似乎没动过，查逻辑 | **诊断**：toybox 后端 + ToyWindow 前端的联动链路——叶瑄不与玩具互动，查是触发缺失还是数据没回流。 | 诊断 | 后端 `core/tools/toybox.py` + client `ToyWindow.tsx` |

### 自主能力

| 编号 | 你的原话 | 底层需求（解读） | 类型 | 落点 |
|---|---|---|---|---|
| **X3** | 让叶瑄自己上网、自己做数据库、自己检索 | 决策项，且最大。已有 `core/tools/web_search.py` 是起点。需评估范围：自主触发上网？建什么库存检索结果？与 X1 向量库是否同一个库？先出决策备忘谈清边界与最小可行版。 | 决策 | `core/tools/web_search.py`、与 X1 共用存储 |

---

## 2. 依赖图（谁挡谁）

```
X1 向量库决策 ──┬─► P3 画像召回（怎么召）
（必须最先拍）   ├─► X2 query relevance 评分（语义 or 词级）
                ├─► G3 溯源（是否向量化）
                └─► X3 自己上网/自建库（是否共用同一存储）

P1 注入骨架 ───┬─► P5 清洗钩子（P1 的子能力）
（结构先定）     ├─► P3 画像单条化（注入侧落点）
                ├─► P2 工具回写（层10改造）
                ├─► P4 加 id（注入侧引用）
                └─► D3 梦境现实丢弃（注入侧 TTL）

G1 日记总结诊断 ─► P2 日记注入（别建在死链路上）
D1 mesexample诊断 ─► D2 impression 大修（梦境簇先探明）
G3 溯源 ◄──► X4 yexuan-self（同一「漂移」问题，二选一或合并）
```

**两个硬前置**：`X1 向量库` 是召回/画像/溯源的根；`P1 注入骨架` 是注入侧所有改动的根。这两个先定，其余才好排。

---

## 3. 建议波次

| 波次 | 内容 | 为什么这个时机 |
|---|---|---|
| **波 0 · 诊断**（快，解锁后续） | G1 日记总结现状、D1 mesexample、G2 显式遗忘盘点、F1/F2/F3 前端三查 | 都是「先查清楚再决定改不改」，产出**结论 + 是否需修**，成本低、能消除后面 brief 的假设风险 |
| **波 1 · 注入骨架** | P1（含 P5 清洗钩子 + 生日尾部注入位的「位置」预留） | 注入侧重构的地基，先定结构，后面内容改造才有落点 |
| **波 2 · 注入内容改造** | P3 画像单条化+tag、P2 工具回写去重、P4 加 id、D3 梦境现实丢弃 | 全部依赖波 1 骨架；可并行 |
| **波 3 · 梦境写入** | D2 dream_impression 大修 | 依赖 D1 诊断结论 |
| **决策轨**（与波 0–1 并行，先出备忘） | X1 向量库、X3 自己上网、G3/X4 溯源与 yexuan-self、X2 评分方向 | 拍板后才能给 P3/X2/G3 定实现方式 |
| **波 4 · 召回质量** | X2 query relevance 评分 | 依赖 X1 决策定方向 |

---

## 4. 决策类项目（下一步出决策备忘，等你拍板）

按你的选择，这几项我会各写一份「**推荐方案 + 取舍 + 成本 + 对现有架构影响**」的决策备忘，你定了再展开实施 brief：

1. **X1 向量库** —— ✅ 已出备忘 `X1-decision-向量库.md`。结论：建、sqlite-vec、embedding 走可插拔接口分阶段；只等你点阶段 A 的 embedding 来源（API / 本地 ONNX）。
2. **X3 叶瑄自己上网 + 自建库 + 自检索** —— 范围最大，需先圈最小可行版。存储选型已被 X1 顺带定了（复用同一 sqlite-vec + `embed()`）。
3. **G3 改动印象溯源 + X4 yexuan-self** —— 合并成一份评估（二者是同一个「漂移可追溯」问题的两种形态）。

---

## 5. 待你确认的歧义点

逐项 brief 前需要你点一下的（不点我就按括号里的默认假设走）：

- **P1**：「单独注入尾部」的「尾部」= author_note（层 11）之后、用户消息之前那一段？（默认：是，紧贴用户消息前最显眼处）。「用户特意提到的事实」由谁标记——你手动标 / 还是系统自动识别「用户主动强调」？（默认：先支持画像里一个 `pinned` 标志位，手动+自动都能写）。
- **P3**：画像条目要不要**整体迁到带 tag+时间戳的新结构**，还是只给「偏好类」加？（默认：先只改造偏好/易变类，稳定事实如职业/地点保留平铺，降风险）。
- **P4 + 前端统一**：「前端统一」指**把破限和世界书放进同一个编辑面板**，还是各自加 id、UI 分开但风格统一？（默认：各自加 id，UI 共用同一套组件但分 tab）。
- **G2 显式遗忘**：触发形态——用户**自然语言说「忘了它」**就生效（要意图识别），还是先做一个**面板里手动删/盖**的显式操作？（默认：先做面板显式操作这条稳的，自然语言遗忘留作二期）。
- ~~**D2 dream_impression「大修」**~~ ✅ 已确认：症状=太模糊给幻觉空间，方向=调细粒度、可记梦境剧情+部分原话。
- ~~**X1 向量库部署约束**~~ ✅ 已确认：无硬约束，包体敏感 + 聊天数据敏感 + 未来上服务器；已出决策备忘，详见 `X1-decision-向量库.md`。

---

> 下一步：你确认波次和上面的歧义点后，我从**波 0 诊断单**或**X1 向量库决策备忘**开始逐个产出。你说先做哪个都行。
